@model BibliotecaNA.Models.Domain.Genero
@{
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <h3>Adicionar Gênero</h3>
            <form asp-action="Add" method="post">
                <div class="form-group">
                    <label asp-for="Name">Nome *</label>
                    <input type="text" class="form-control" asp-for="Name" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                @if (TempData["msg"] != null)
                {
                    <div class="form-group">
                        @TempData["msg"]
                    </div>
                }
                <div class="form-group pt-3">
                    <button type="submit" class="btn btn-dark"><i class="bi bi-save"></i> Adicionar</button>
                    <a href="/Genre/GetAll" class="btn btn-primary"> <i class="bi bi-list"></i>Listar gêneros</a>
                </div>

            </form>
        </div>
    </div>
</div>