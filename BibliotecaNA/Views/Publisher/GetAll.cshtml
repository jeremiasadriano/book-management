@model IEnumerable<BibliotecaNA.Models.Domain.Publisher>
@{
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-dark" href="/Publisher/Add">Adicionar mais editoras</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6">
            <h3>Publishers</h3>

            <table class="table table-stripped table-bordered table-dark">
                <thead>
                    <tr>
                        <th>Nome da editora</th>
                        <th>Ação</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.PublisherName</td>
                            <td>
                                <a class="btn btn-success" href="/Publisher/Update?id=@item.Id"><i class="bi bi-pen"></i>
                                    Editar</a> |
                                <a class="btn btn-danger"
                                    onclick="return window.confirm('Tem a certeza que deseja remover?')"
                                    href="/Publisher/Delete?id=@item.Id"><i class="bi bi-trash"></i> Remover</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
